---
- hosts: all
  gather_facts: false
  tasks:
     - name: this is a task !
       ping:
     - name: test facts
       debug: 
         msg: "this is our provider :: {{ provider }}"
     - name: test facts
       debug: 
         msg: "message two !!"
